apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3wwww.fullname" . }}
  labels:
    {{- include "s3wwww.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3wwww.selectorLabels" . | nindent 4 }}
